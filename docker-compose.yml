version: "3.9"
services:
  sanednf:
    build: .
    restart: always
    ports:
      - "3000:3000"
    networks: 
      - reverse-proxy
      - back
networks:
  reverse-proxy:
    external:
      name: reverse-proxy
  back:
    driver: bridge
  host:
    driver: host
